// SEM-BORROW-03: invalid overlapping mutable borrows use E3003.
struct Owner<T> {
    value: T,
};

fn main() {
    dec rw owner = Owner<i32> { value: 1 };
    dec first = &rw owner.value;
    dec second = &rw owner.value;
    *first = *second;
}
