#[derive(PartialEq)]
struct Point {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    dec pt1 = Point { x: 10, y: 20 };
    dec pt2 = Point { x: 10, y: 20 };
    dec is_same = pt1.eq(&pt2);
    if is_same {
        return 0;
    }
    return 1;
}
