#[derive(Debug, Clone, PartialEq, Eq, Default)]
struct Vector {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    dec v1 = Vector { x: 1, y: 2 };
    dec v2 = v1.clone();
    dec eq = v1.eq(&v2);
    return v2.x;
}
