// typeof as a type-level query — positive test
// No errors expected

type IntAlias = typeof(42);

fn make_value() -> i32 {
    return 10;
}

type ValueType = typeof(make_value());

fn use_typeof() {
    dec x: typeof(42) = 100;
}

fn main() {}
