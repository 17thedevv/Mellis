import os
import sys

def dump():
    print("running")
    # Actually, I can just use Python to run a cargo command if I set up a small crate, but it's easier to just add a println to mellis-driver/src/lib.rs instead.

if __name__ == "__main__":
    dump()
