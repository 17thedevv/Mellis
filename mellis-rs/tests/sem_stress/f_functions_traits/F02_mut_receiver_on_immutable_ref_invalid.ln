// [STRESS: COMPILE_REJECT: Cannot call method requiring mutable receiver]
struct Counter {
    val: i32,
};

impl Counter {
    fn inc(self: &rw Self) {
        self.val = self.val + 1;
    }
}

fn main() -> i32 {
    dec c = Counter { val: 41 };
    dec r = &c;
    r.inc();
    return c.val;
}
