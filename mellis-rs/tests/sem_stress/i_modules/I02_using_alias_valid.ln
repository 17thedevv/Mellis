// [STRESS: COMPILE_ACCEPT]
// [STRESS: EXEC_EXIT: 42]
module math {
    module ops {
        export fn add(a: i32, b: i32) -> i32 {
            return a + b;
        }
    }
}

using math::ops as op;

fn main() -> i32 {
    return op::add(40, 2);
}
