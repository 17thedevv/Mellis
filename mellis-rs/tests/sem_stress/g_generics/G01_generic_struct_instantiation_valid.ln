// [STRESS: COMPILE_ACCEPT]
// [STRESS: EXEC_EXIT: 42]
struct Pair<T, U> {
    first: T,
    second: U,
};

fn make_pair<T, U>(a: T, b: U) -> Pair<T, U> {
    return Pair { first: a, second: b };
}

fn main() -> i32 {
    dec p = make_pair<i32, i32>(20, 22);
    return p.first + p.second;
}
