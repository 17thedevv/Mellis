// [STRESS: COMPILE_ACCEPT]
// [STRESS: EXEC_EXIT: 42]
struct Wrap<T> {
    val: T,
};

fn unwrap_nested<T>(w: Wrap<Wrap<T>>) -> T {
    return w.val.val;
}

fn main() -> i32 {
    dec inner = Wrap { val: 42 };
    dec outer = Wrap { val: inner };
    return unwrap_nested<i32>(outer);
}
