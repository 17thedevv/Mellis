import <core>;
import <alloc>;

fn test_hashset_if() -> i32 {
    dec rw set = hashset_new<u64>();
    dec rw b = true;
    if b {
        dec x = set.into_iter();
    }
    return 0;
}

export fn main() -> i32 {
    test_hashset_if();
    return 0;
}
