import <string>;

fn main() -> i32 {
    dec rw s = string_from_str("Hi");
    s.push_char('!');
    s.push_str(" Luna");
    if s.len() != (8 as u64) {
        return 1;
    }
    s.truncate(3 as u64);
    if s.len() != (3 as u64) {
        return 2;
    }
    return 0;
}
