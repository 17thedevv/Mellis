// SEM-VIS-01 provider: frozen VIS-STRUCT makes this unmodified field private.
export struct Secret {
    value: i32,
}

export fn make_secret(value: i32) -> Secret {
    return Secret { value: value };
}
