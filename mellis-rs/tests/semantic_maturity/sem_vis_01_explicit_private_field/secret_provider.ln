// SEM-VIS-01 provider: fields are public by default; `private` is explicit.
export struct Secret {
    private value: i32,
};

export fn make_secret(value: i32) -> Secret {
    return Secret { value: value };
}
