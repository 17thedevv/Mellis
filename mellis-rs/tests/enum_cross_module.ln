// Test: import core and use Option::is_some method
import <core>;

fn main() -> i32 {
    dec x: core::Option<i32> = core::Option::Some(42);
    if x.is_some() {
        return 1;
    }
    return 0;
}
