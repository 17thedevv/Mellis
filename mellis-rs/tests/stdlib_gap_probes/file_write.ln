import <io>;
import <string>;
import <result>;

fn main() -> i32 {
    // Attempting to write bytes or text to a file
    dec s = string_from_str("data to write");
    dec res = io::write_file("output.txt", s.as_bytes());
    return 0;
}
