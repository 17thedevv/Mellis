import <hashmap>;
import <string>;

fn main() -> i32 {
    // Attempting to count word frequencies with HashMap<String, i32>
    dec rw map = hashmap_new<String, i32>();
    dec word = string_from_str("hello");
    map.insert(word, 1);
    return 0;
}
